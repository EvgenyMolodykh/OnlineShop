<UserControl x:Class="WeatherAppWPF.Views.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherAppWPF.Views.DayFarecastDetails"
             xmlns:vm="clr-namespace:WeatherAppWPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:HomeViewViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="0">
                        <local:DayFarecastDetailsView DataContext="{Binding SelectedDay}"/>
                    </Grid>

                    <ListBox x:Name="HourlyForecast_ListBox" Grid.Column="1">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical"></StackPanel>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Path=Time}"/>
                                    <Label Content="{Binding Path=ApparentTemperature}"/>
                                    <Label Content="{Binding Path=RelativeHumidity}"/>
                                    <Label Content="{Binding Path=WeekDay}"/>
                                    <Label Content="{Binding Path=Wheather}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>


                <ListBox x:Name="WeaterDays_ListBox" 
                         Grid.Row="1" 
                         ItemsSource="{Binding ForecastsDays }"
                         SelectedItem="{Binding SelectedDay}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            
                                <!--Style="{StaticResource ButtonPanelButton}" вставить ресурсы -->
                                <StackPanel Orientation="Vertical">
                                    <Label Content="{Binding Path=DataTime}"/>
                                    <Label Content="{Binding Path=MaxTemperature}"/>
                                    <Label Content="{Binding Path=MinTemperature}"/>
                                    <Label Content="{Binding Path=WeekDay}"/>
                                    <Label Content="{Binding Path=Wheather}"/>
                                </StackPanel>
                           
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

        </Grid>
    </Grid>
</UserControl>
